global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'node_exporter'
    static_configs:
      - targets: ['node_exporter:9100']

  # Example placeholder for external app metrics
  - job_name: 'external_app'
    static_configs:
      - targets: ['host.docker.internal:9090']  # Update based on app
